#include <stdio.h>

/**
 * main - prints alpha in lower case
 *
 * Return :always 0
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++i)
	{
		putchar(lc);
	}
	putchar ('\n');
	return (0);
}
